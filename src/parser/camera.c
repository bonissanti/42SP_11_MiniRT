#include "../../include/minirt.h"

// int	camera(char *line, t_data *data)
// {
// 	char **token;
//
// 	token = ft_split(token, ' ');
// 	if (count_token(token, 2, "Error\nCamera has wrong number of arguments\n");
// 		return (1);
// 	if (set_coordinates(token[0], data) != 0)
// 	{
// 		free_array(token);
// 		return (1);
// 	}
// 	if (set_normalized_3d(token[1], data) != 0)
// 	{
// 		free_array(token);
// 		return (1);
// 	}
// 	if (set_fov(token[1], data) != 0)
// 	{
// 		free_array(token);
// 		return (1);
// 	}
// 	free_array(token);
// 	return (0);
// }
